import React from 'react';

const Cars = ({car}) => {
    return (
        <div>
          <header>
          {/* Save space for NavBar */}
          </header>

        </div>
    );
}

export default Cars;